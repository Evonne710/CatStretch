# 🎨 CatStretch 文字样式配置指南

## 📍 样式位置

所有文字样式在 `CatWindow.swift` 的 `show()` 方法中设置。

---

## 🎯 当前文字样式

### 1️⃣ 计时标签（顶部）
```swift
// 当前设置：
字体：PingFangSC-Medium（苹方中等粗细）
大小：40pt
颜色：纯白色
阴影：黑色半透明 + 8px模糊 + 2px偏移
位置：距离顶部 120px
```

### 2️⃣ 提示文字（中间）
```swift
// 当前设置：
字体：PingFangSC-Regular（苹方常规）
大小：28pt
颜色：暖白色 (RGB: 1.0, 0.9, 0.8)
阴影：黑色半透明 + 6px模糊 + 1px偏移
位置：屏幕中间偏上 250px
```

### 3️⃣ 紧急退出进度（底部）
```swift
// 当前设置：
字体：PingFangSC-Regular
大小：16pt
颜色：淡红色 (RGB: 1.0, 0.7, 0.7)
位置：距离底部 15px
```

---

## 🔧 如何自定义样式

### 修改字体

```swift
// 方式 1：使用系统字体
timerLabel.font = NSFont.systemFont(ofSize: 40, weight: .bold)

// 方式 2：使用苹方字体（推荐中文）
timerLabel.font = NSFont(name: "PingFangSC-Regular", size: 40)
timerLabel.font = NSFont(name: "PingFangSC-Medium", size: 40)
timerLabel.font = NSFont(name: "PingFangSC-Semibold", size: 40)

// 方式 3：使用圆体（更可爱）
timerLabel.font = NSFont(name: "YuantiSC-Regular", size: 40)

// 方式 4：使用宋体（更文艺）
timerLabel.font = NSFont(name: "SongtiSC-Regular", size: 40)

// 方式 5：使用手写体
timerLabel.font = NSFont(name: "STXingkai", size: 40)
```

### 修改颜色

```swift
// 纯白色
timerLabel.textColor = .white

// 暖白色（温馨感）
timerLabel.textColor = NSColor(calibratedRed: 1.0, green: 0.9, blue: 0.8, alpha: 1.0)

// 粉色（可爱感）
timerLabel.textColor = NSColor(calibratedRed: 1.0, green: 0.7, blue: 0.8, alpha: 1.0)

// 浅蓝色（清新感）
timerLabel.textColor = NSColor(calibratedRed: 0.7, green: 0.8, blue: 1.0, alpha: 1.0)

// 浅黄色（温暖感）
timerLabel.textColor = NSColor(calibratedRed: 1.0, green: 0.95, blue: 0.7, alpha: 1.0)

// 渐变色效果（需要自定义绘制）
// 这个比较复杂，需要用到 NSGradient
```

### 修改阴影

```swift
let shadow = NSShadow()

// 阴影颜色
shadow.shadowColor = NSColor.black.withAlphaComponent(0.5)  // 半透明黑色

// 阴影模糊程度
shadow.shadowBlurRadius = 8  // 越大越模糊

// 阴影偏移
shadow.shadowOffset = NSSize(width: 2, height: 2)  // 向右下偏移

timerLabel.shadow = shadow
```

### 修改大小

```swift
// 计时标签
timerLabel.frame = NSRect(x: 0, y: Int(rect.height) - 120, width: Int(rect.width), height: 60)

// 提示文字
hintLabel.frame = NSRect(x: 0, y: Int(rect.height / 2) + 250, width: Int(rect.width), height: 50)
```

---

## 🎨 预设风格方案

### 方案 A：可爱风格
```swift
// 计时标签
timerLabel.font = NSFont(name: "YuantiSC-Regular", size: 44)
timerLabel.textColor = NSColor(calibratedRed: 1.0, green: 0.8, blue: 0.9, alpha: 1.0)  // 粉色

// 提示文字
hintLabel.font = NSFont(name: "YuantiSC-Regular", size: 30)
hintLabel.textColor = NSColor(calibratedRed: 1.0, green: 0.9, blue: 0.8, alpha: 1.0)  // 暖白
```

### 方案 B：文艺风格
```swift
// 计时标签
timerLabel.font = NSFont(name: "SongtiSC-Regular", size: 42)
timerLabel.textColor = NSColor(calibratedRed: 0.9, green: 0.85, blue: 0.8, alpha: 1.0)  // 米色

// 提示文字
hintLabel.font = NSFont(name: "SongtiSC-Regular", size: 26)
hintLabel.textColor = NSColor(calibratedRed: 0.8, green: 0.75, blue: 0.7, alpha: 1.0)  // 浅棕
```

### 方案 C：现代风格
```swift
// 计时标签
timerLabel.font = NSFont.systemFont(ofSize: 48, weight: .light)
timerLabel.textColor = .white

// 提示文字
hintLabel.font = NSFont.systemFont(ofSize: 24, weight: .regular)
hintLabel.textColor = NSColor(calibratedRed: 0.9, green: 0.9, blue: 0.9, alpha: 1.0)  // 浅灰白
```

### 方案 D：醒目风格
```swift
// 计时标签
timerLabel.font = NSFont(name: "PingFangSC-Semibold", size: 52)
timerLabel.textColor = NSColor(calibratedRed: 1.0, green: 0.95, blue: 0.5, alpha: 1.0)  // 亮黄

// 提示文字
hintLabel.font = NSFont(name: "PingFangSC-Medium", size: 32)
hintLabel.textColor = .white
```

---

## 📐 位置调整

```swift
// 屏幕坐标系（原点在左上角）
// x: 从左到右
// y: 从下到上

// 计时标签位置（距离顶部）
timerLabel.frame = NSRect(x: 0, y: Int(rect.height) - 120, ...)  
// ↑ 120 越大，越往下

// 提示文字位置（距离中间）
hintLabel.frame = NSRect(x: 0, y: Int(rect.height / 2) + 250, ...)
// ↑ 250 越大，越往上
```

---

## 💡 快速测试方法

1. 在 Xcode 中修改样式
2. 按 `Cmd + R` 运行
3. 菜单栏 > 🔔 立即提醒
4. 查看效果
5. 不满意继续调整

---

## 🎯 推荐字体列表

| 字体名称 | 风格 | 适用场景 |
|---------|------|---------|
| `PingFangSC-Regular` | 现代简洁 | 日常使用 |
| `PingFangSC-Medium` | 现代清晰 | 计时标签 |
| `YuantiSC-Regular` | 圆润可爱 | 猫咪主题 |
| `SongtiSC-Regular` | 文艺复古 | 文艺风格 |
| `STXingkai` | 手写书法 | 特殊效果 |
| `STXiaoxi` | 卡通童趣 | 可爱风格 |

---

## 🔥 进阶效果

### 文字发光效果
```swift
let glowShadow = NSShadow()
glowShadow.shadowColor = NSColor(calibratedRed: 1.0, green: 0.8, blue: 0.9, alpha: 0.8)
glowShadow.shadowBlurRadius = 15
glowShadow.shadowOffset = NSSize(width: 0, height: 0)
timerLabel.shadow = glowShadow
```

### 多重阴影（立体效果）
```swift
// 需要自定义 NSView 绘制，比较复杂
```

---

告诉我你想要哪种风格，或者想调整哪些属性，我可以帮你快速实现！
